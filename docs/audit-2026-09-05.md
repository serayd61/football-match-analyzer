# Denetim uygulaması — 5 Eylül 2026

Başlangıç: `c35fd04` (main). Dal: `audit/2026-09-05-contract-and-frontend`. Üretime yayın, üretim migration'ı veya kayıt silme YAPILMADI. Veritabanı yalnız okundu.

## 1. Bulgu → kanıt → durum

| # | Bulgu | Güncel kodda doğrulama (c35fd04) | Bu dalda yapılan |
|---|-------|----------------------------------|------------------|
| P0 | Kimliksiz test yazımı | `api/test-unified-save/route.ts` GET: auth yok, service-role upsert+delete; middleware matcher kapsamıyor | Üretimde 404 (`lib/api/dev-only.ts`), yalnız `CRON_SECRET/ADMIN_SECRET` Bearer ile açılır. Aynı koruma `api/performance/debug` (test kaydı yazıp siliyor). |
| P1 | Sürüm belirsizliği | `getPrediction` `limit(1)` sırasız; liste/sonuç/performans sürüm ayırmıyor. DB: 10.687 satır, hepsi `dc-1.0` (gizli risk) | `lib/site/official.ts`: `SITE_MODEL_VERSION` ile deterministik seçim, yoksa fixture başına en son `updated_at`. Tüm site sorgularına uygulandı (liste, detay, form, H2H, sonuçlar, performans, kalibrasyon cronu). |
| P1 | Kalibrasyon eğitim=değerlendirme | `fit-calibration`: `brier_after` aynı `pts` üzerinde | Cron artık in-sample'ı "eğitim ölçümü" diye etiketler, kronolojik %20 holdout ekler; performans sayfası aylık walk-forward (yalnız önceki aylar) gösterir. Sütun taslağı: `supabase/migrations/2026-09-05_confidence_calibration_holdout.sql`. |
| P1 | Boş → 0 | `Number(r.p_over25)` null→0 → "%100 Alt" örneği. DB'de bugün null yok (gizli risk) | `lib/calibration-eval.finiteOrNull`: null/''/NaN reddedilir; pazar başına geçerli örnek sayısı raporlanır. |
| P1 | Doğrulama zayıf | Ingest yalnız sonlu sayı; `parseRows` tek hatalı satırda tüm günü boşaltır | Ingest Zod: id/tarih, 0–1, toplam 1±0.02, lambda≥0, pick=argmax, kickoff geçmişse ret (`allowPastKickoff` ile açık izin), satır bazlı `rejected[]`, tümü geçersizse 422. Site: satır bazlı karantina + toplam 1±0.03. |
| P1 | Form/H2H tarih kesimi yok | `getTeamForm/getHeadToHead` `before` filtresi yok | `before` parametresi (`lib/site/asof.ts`), maç sayfası kendi kickoff'unu geçer; kaynak sınırı UI'da yazılı. |
| P1 | Oran kapsamı | `fetchOdds` genel en yeni 5.000 satır (DB'de 455); `correct=null` → lost | Oranlar aynı fixture kümesi için 200'lük parçalarla çekilir; açılış/kapanış ROI ayrı, payda/kapsam/eksik/sağlayıcı görünür; `correct=null` skordan türetilir ve sayılır. |
| P1 | Durum kaybı | `fixtureRow` LIVE/FT → pending; ana sayfa `!settled` | `status` (scheduled/live/finished/postponed/cancelled/unknown) ve `modelStatus` ayrı; feed durumu puanlı satırlara da işlenir; ana sayfa yalnız `scheduled`. Feed hatası ile boş takvim ayrı gösterilir. |
| P2 | Tablet sütunları | md grid ≈ 836px, 768'de yatay taşma (üretimde ölçüldü: scrollWidth 879/768) | Grid `lg`'de açılır; altında yığın düzeni + Ü/A ve KG satır içi. Başlık da `lg`'ye taşındı (885px taşıyordu). |
| P2 | Arama/durum filtresi | Yalnız date/league/scope | `q`, `status`, `ready`, `sort` URL'de; temizle; filtreli boş sonuç ayrı. |
| P2 | Kaynak karışıklığı | Value radar KG'yi Poisson'dan türetiyordu | Kayıtlı `p_btts_yes` birincil, Poisson yalnız yedek; `source` etiketi. Maç sayfası "kayıtlı / türetilmiş" ve ham/kalibre ayrımını yazar. |

## 2. Aktif mimari (import/çağrı zinciri ile)

| Ekran | Route | Veri fonksiyonu | Tablo/API | Üretici | Güncelleme |
|-------|-------|-----------------|-----------|---------|------------|
| Ana sayfa | `(site)/[locale]/page.tsx` | `listDay`, `getPerformance`, `listResults` | `engine_predictions`, `prediction_odds`, feed (Redis) | predict-service (Python DC) → n8n → `/api/v2/predictions/ingest` | ISR 15 dk; ingest günlük ~04:00 UTC |
| Tahminler | `predictions/page.tsx` | `listDay` + `applyFilters` | aynı | aynı | ISR 15 dk |
| Maç | `predictions/[matchId]` | `getPrediction`, `getMarketSnapshot`, `getMarketBook`, `getTeamForm(before)`, `getHeadToHead(before)` | `engine_predictions`, `prediction_odds` | ingest + `cron/snapshot-odds` (saatlik) | ISR 15 dk |
| Sonuçlar | `results/page.tsx` | `listResults` | `engine_predictions` | `cron/settle-engine` (saatlik, 7 gün sonra void) | ISR 5 dk |
| Performans | `performance/page.tsx` | `getPerformance` | `engine_predictions`, `prediction_odds`, `confidence_calibration` | settle + snapshot-odds + `cron/fit-calibration` (haftalık) | ISR 60 dk |
| Panel | `dashboard/page.tsx` (dynamic) | `listDay`, `valueRadar`, `readWatchlist` | + `site_watchlist` | — | istek başına |

Aktif model yolu: yalnız Python Dixon-Coles (`engine/service.py`, `MODEL_VERSION=dc-1.0`). `lib/quad-brain`, `unified-consensus`, `autolearn`, `smart-analyzer` yalnız eski `(app)` rotaları ve `cron/daily-featured` / `v2/queue-daily` (eski `smart_analysis`/`unified_analysis` tabloları) tarafından çağrılır; public site bunlara bağlı değildir. Dağıtım commit'i doğrulanmadı.

## 3. Service-role ile yazan ve doğrudan auth görünmeyen uç noktalar (envanter)

Middleware `/api/admin/*` ve `/admin/*` yollarını korur. Aşağıdakiler matcher dışında ve dosya içinde oturum/secret kontrolü bulunamadı (statik tarama; bazıları gövde içinde başka koruma taşıyor olabilir):

`api/auto-predict`, `api/analyze-deepseek-master`, `api/deepseek-master`, `api/deepseek-evaluate`, `api/cron/auto-analyze-matches`, `api/cron/settle-unified`, `api/unified/settle`, `api/v2/settle`, `api/performance/recalculate`, `api/contact`, `api/unsubscribe`, `api/auth/register` (kayıt akışı), `api/stripe/webhook` (imza doğrulaması gövdede beklenir). Bunlar eski uygulama yüzeyidir; bu dalda dokunulmadı — ayrı iş.

## 4. Testler

`npm test` → 26/26 (node:test + tsx, yeni bağımlılık yok). Kapsam: null/boş/geçersiz olasılık ve toplam; iki sürümde tek resmi kayıt; ingest'in settlement alanlarına dokunmaması; holdout/walk-forward; form kesimi; oran kapsamı/null settlement; LIVE/FT/model-pending birleşimi; üretimde yetkisiz test uç noktası. Tarayıcı akışları (mobil/tablet/dil) ekran görüntüsüyle elle doğrulandı; otomatik e2e yok. `next lint` yapılandırılmamış (etkileşimli kurulum ister) — çalıştırılmadı. `tsc --noEmit` temiz; `next build` başarılı.

## 5. Kalan riskler ve sıradaki üç iş

1. `engine_prediction_history` tetikleyicisini ve `confidence_calibration` sütunlarını Supabase'de uygula (taslaklar `supabase/migrations/`); ardından n8n'e `allowPastKickoff` gerekip gerekmediğini kontrol et.
2. Eski uygulama uç noktalarındaki kimliksiz service-role yazımlarını tek tek kapat veya secret'a bağla (bölüm 3).
3. xG/Elo raporları yeniden üretilmedi; canlı model hâlâ `dc-1.0`. xG'yi canlıya almadan önce `engine/backtest_xg.py` sonuçlarını belirsizlik aralığıyla tekrar üret; form tilt eklenmesin.
